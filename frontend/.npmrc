# Keep frontend's own node_modules independent from workspace root hoisting.
# This prevents root-level npm install from deduplicating jsdom and other
# test-only packages out of frontend/node_modules.
workspaces=false
